export const contactCode = state => state.buyer.contactCode || '';

export const nickname = state => state.buyer.nickname || '';

export const offerId = state => state.buyer.offerId;

export const assetQty = state => state.buyer.assetQty;

export const fiatQty =state => state.buyer.fiatQty;
